# Oxford Spires long sequences (stride 1) — lingbot_map ATE-only evaluation.
# For the short-sequence pose + AUC benchmark, use configs/oxford.yaml.
workspace: /NHNHOME/WORKSPACE/26msit001_A/bispl_lab/youngmin/bench_ws/oxford_long

datasets:
  - oxford_long

methods:
  - base_h64
  - sd_a1_h64

evaluation:
  traj:
    enable: true
    vis: true
  auc:
    enable: false
  depth:
    enable: false
  points:
    enable: false
