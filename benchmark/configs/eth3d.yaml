# ETH3D — lingbot_map trajectory + point-cloud evaluation.
# DA3 (DepthAnything3) split; see configs/datasets/eth3d.yaml.
workspace: /NHNHOME/WORKSPACE/26msit001_A/bispl_lab/youngmin/bench_ws/eth3d

datasets:
  - eth3d

methods:
  - base_h64
  - sd_a1_h64

evaluation:
  traj:
    enable: true
    vis: true
  auc:
    enable: true
    vis: true
    aggregation: both
  depth:
    enable: false
  points:
    enable: true
    vis: false
    icp_threshold: 0.1
