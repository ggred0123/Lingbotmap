# Neural RGB-D — lingbot_map point-cloud evaluation.
# Indoor sequences with GT depth + mesh for surface reconstruction.
workspace: /NHNHOME/WORKSPACE/26msit001_A/bispl_lab/youngmin/bench_ws/neural_rgbd

datasets:
  - neural_rgbd

methods:
  - base_h64
  - sd_a1_h64

evaluation:
  traj:
    enable: false
  auc:
    enable: false
  depth:
    enable: false
  points:
    enable: true
    vis: false
    icp_threshold: 0.1
