# 7Scenes — lingbot_map trajectory + AUC + point-cloud evaluation.
# Stride-5 sampling; see configs/datasets/seven_scenes.yaml.
workspace: /NHNHOME/WORKSPACE/26msit001_A/bispl_lab/youngmin/bench_ws/seven_scenes

datasets:
  - seven_scenes

methods:
  - base_h64
  - sd_a1_h64

evaluation:
  traj:
    enable: true
    vis: true
  auc:
    enable: true
    vis: true
    aggregation: both
  depth:
    enable: false
  points:
    enable: true
    vis: false
    icp_threshold: 0.1
    conf_threshold: 0.0
