# gtlam step275 vs base on kitti, same protocol gtsup and teasup were scored on.
workspace: /NHNHOME/WORKSPACE/26msit001_A/bispl_lab/youngmin/bench_ws/kitti
datasets:
  - kitti_504x280
methods:
  - base_auto
  - sd_gtlams275_auto
evaluation:
  traj: {enable: true, vis: false}
  auc: {enable: true, vis: false, aggregation: both}
  depth: {enable: false}
  points: {enable: false}
