# Is there ANY re-anchor period that recovers the trained checkpoint?  M=64 left
# v6i's ATE unchanged (+0.023); these bracket it on both sides.
workspace: /NHNHOME/WORKSPACE/26msit001_A/bispl_lab/youngmin/bench_ws/oxford_long
datasets:
  - oxford_long
methods:
  - sd_v6is1200_rk32_auto
  - sd_v6is1200_rk128_auto
evaluation:
  traj: {enable: true, vis: false}
  auc: {enable: true, vis: false, aggregation: both}
  depth: {enable: false}
  points: {enable: false}
